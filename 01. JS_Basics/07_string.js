const name = "abhishek";
const repocnt = 10;

console.log(`Hello my name is ${name} and my repo count is ${repocnt}`);

// decalaration

const game = new String("abhishek");

// methods of string

// .length : find length of String
// .tolowerCase : change into lower case
// .uppercase : change into uppercase
// .charAt : find the position of character in string => console.log(name.charAt(2))

// .indexof : find the position of character
// .substring(start , end) it will slice the string from the given position

// .slice : it can have -ve value that you can slice through reverse

// .trim : it will remove all the whitespaces and their is trim.start and trim.end

// replace : url.replace('charactertoreplace' , 'replacewith')

// const url = "https://stackoverflow.com/questions/23412938/node-is-not-recognized-as-an-internal-or-an-external-command-operable-program"

// console.log(url.replace('/' , '3'))

// .includes : it will check whether the charatcter is thier or not

// .split : console.split('seperator');
